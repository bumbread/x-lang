{

  :int x = 2;
  :int y = 3;
  :int z = x + y*123;
  print z / 2;

  if z < 256 {
    x = x + 3;
    while true {
      y = y - 1;
      if y > 2 { break; }
    }
  }

  :int<-(int, int, int) b;
  :int$[] a;

  

}
